---
import { Icons, type IconDefinition, type IconName } from './icons';

/**
 * Renders one glyph from `icons.ts`. `name` is typed, so a typo fails
 * `astro check`; `class` and `title` come from the call site, where the
 * surrounding layout is known.
 */
interface Props {
  name: IconName;
  class?: string;
  /** Accessible name; omit for purely decorative icons. */
  title?: string;
}

const { name, class: className, title } = Astro.props;

const icon: IconDefinition = Icons[name];
---

<svg
  class={className}
  width={icon.width}
  height={icon.height}
  viewBox={icon.viewBox}
  fill={icon.fill}
  fill-rule={icon.fillRule}
  clip-rule={icon.clipRule}
  stroke={icon.stroke}
  stroke-width={icon.strokeWidth}
  stroke-linecap={icon.strokeLinecap}
  stroke-linejoin={icon.strokeLinejoin}
  aria-hidden={title ? undefined : 'true'}
>
  {title && <title>{title}</title>}
  {icon.paths.map(path => (
    <path d={path.d} class={path.class} />
  ))}
</svg>
