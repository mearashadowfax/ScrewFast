---
import ProductView from '@views/ProductView.astro';
import { staticPathsFor } from '@utils/content';

export const getStaticPaths = () => staticPathsFor('products', 'de');

const { entry } = Astro.props;
---

<ProductView product={entry} />
