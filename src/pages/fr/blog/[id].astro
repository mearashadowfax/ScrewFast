---
import BlogPostView from '@views/BlogPostView.astro';
import { staticPathsFor } from '@utils/content';

export const getStaticPaths = () => staticPathsFor('blog', 'fr');

const { entry } = Astro.props;
---

<BlogPostView post={entry} />
