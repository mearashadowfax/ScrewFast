---
import InsightView from '@views/InsightView.astro';
import { staticPathsFor } from '@utils/content';

export const getStaticPaths = () => staticPathsFor('insights', 'fr');

const { entry } = Astro.props;
---

<InsightView post={entry} />
